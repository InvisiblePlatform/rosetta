{{- $.Scratch.SetInMap "index" "" (dict "" "" ) -}}
{{- range where .Site.Pages "Type" "not in"  (slice "json") -}}
    {{- $tempslice := (slice .Params.title ) -}}
{{- end -}}
{{- $.Scratch.Get "index" | jsonify -}}
